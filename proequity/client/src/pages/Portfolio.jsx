import React from 'react';
import GlassCard from '../components/shared/GlassCard';
import { useAuth } from '../hooks/useAuth';
export default function Portfolio({ setAuthOpen }) { const { user } = useAuth(); return <div className="tab-pane active"><GlassCard className="panel"><div className="panel-title">Portfolio</div>{user ? <p style={{ color: 'var(--text-secondary)', marginTop: 16 }}>Your portfolio data connection is ready to be migrated to provenance-aware quotes. Current values are intentionally withheld until an approved market-data provider is configured.</p> : <p style={{ color: 'var(--text-secondary)', marginTop: 16 }}>Log in to view and manage your own holdings. No sample portfolio is displayed.</p>} {!user && <button className="btn btn-glow" onClick={() => setAuthOpen(true)}>Login</button>}</GlassCard></div>; }
